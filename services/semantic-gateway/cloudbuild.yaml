steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - services/semantic-gateway/Dockerfile
      - --tag
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}

options:
  logging: CLOUD_LOGGING_ONLY
